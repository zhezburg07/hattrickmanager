import { currentTimestampLabel } from "@/data/dashboard";
import styles from "./Updates.module.css";

// Раньше здесь была полностью выдуманная симуляция очереди обновлений (с
// таймером и прогресс-баром), явно помеченная на странице как демонстрация.
// Реальной фоновой системы очередей нет и не планируется в обозримом
// будущем — вместо обещания того, чего нет, честно объясняем, как это
// работает на самом деле: данные запрашиваются у CHPP заново при каждом
// заходе в раздел личного кабинета.
export default function UpdatesSection() {
  return (
    <div className={styles.card}>
      <div className={styles.cardTitle}>Обновления</div>

      <p className={styles.statusLine}>
        Последнее обновление интерфейса: <b>{currentTimestampLabel()}</b>
      </p>

      <p className={styles.explainText}>
        Отдельной фоновой системы обновлений нет — HattrickManager запрашивает у Hattrick свежие данные напрямую
        каждый раз, когда вы открываете раздел личного кабинета (Обзор, Состав, Финансы и т.д.). Просто зайдите на
        нужную вкладку ещё раз, чтобы увидеть актуальное состояние.
      </p>
    </div>
  );
}
